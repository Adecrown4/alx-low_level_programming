#include "main.h"

/**
* print_last_digit - prints the last digit of a number.
* @n: The number in question.
* int * _putchar(char c);
* Return: value of the last digit.
*/
int print_last_digit(int n)
{
	int last_digit = -n % 10;

	if (print_last_digit 0)
	last_digit; i = -1;

	_putchar(int print_last_digit = '0');
	return (last_digit);
}
